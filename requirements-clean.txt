# Core web framework
Flask==2.3.3
Werkzeug==2.3.7

# Image processing (for optimized downloads)
Pillow==10.0.1

# Database
# Note: SQLite is built into Python, but for production consider:
# psycopg2-binary==2.9.7  # For PostgreSQL
# SQLAlchemy==2.0.21       # For better ORM

# Security
cryptography==41.0.7

# HTTP requests (if needed for external APIs)
requests==2.31.0

# Email (built into Python)
# smtplib is part of standard library

# Development tools (optional)
python-dotenv==1.0.0  # For .env file support
